# 简单反代 Iwara（I 站）视频

一个用于“前端 + Cloudflare Worker + Node/Python 后端”三种方式部署的 **轻量级 Iwara 视频反代工具**。

------

## 📦 项目主要文件说明

本项目提供三种可供选择的部署方式（Worker、第三方反代、完全私有），对应文件职责如下：

| 文件                    | 说明                                                         |
| ----------------------- | ------------------------------------------------------------ |
| **index.html**          | Iwara 视频反代的前端界面，基于 HTML/CSS/JS 简单构建。        |
| **worker.js**           | Cloudflare Worker 版本入口文件，包含前端模板和反代逻辑，可实现零服务器部署。 |
| **server.js**           | Node.js 后端版本，结合 index.html 可搭建自托管服务。         |
| **server.py**           | Python 后端版本，功能与 Node.js 版本相同。                   |
| **allOrigins-main.zip** | Github开源反代Nodejs项目。                                   |
| **sweetalert.min.js**   | Github开源基于JS封装的可以自定义美化的提示框。               |

------

## 🚀 部署方式（推荐程度从高到低）

### 1️⃣ Cloudflare Worker 部署（最推荐、最快速、最稳定）

Cloudflare Worker 不需要服务器，也不会暴露真实 IP，是本项目的**首选部署方式**。

#### 部署步骤

1. 下载Worker部署文件夹下的worker.js文件
2. 注册 Cloudflare 帐号（邮箱即可）
3. 在 Cloudflare Dashboard → Workers & Pages → **创建 Worker**
4. 将 **Worker部署/worker.js** 全部内容复制到编辑器
5. 保存并部署即可立即使用
6. **强烈建议添加自定义域名**（非常重要）

------

#### ⚠ 为什么必须自定义域名？

Cloudflare Worker 默认提供的 `*.workers.dev` 域名
 在 **中国大陆会被 DNS 污染，无法访问**
 因此请务必绑定你托管在 Cloudflare 上的域名。

> **正确做法：**
>  Cloudflare → Worker → 触发器 → 自定义域 → 添加你自己的域名
>
> 绑定后即可在国内正常访问。

------

### 2️⃣ 第三方反代部署（快速，但依赖外部服务）

因需绕过Cloudflare的人机挑战，需要依赖第三方代理接口：

```
https://api.allorigins.win/raw?url=
```

如果你只需要快速部署（非中国大陆地区），可使用此方式。

#### 部署步骤：

1. 下载 `第三方反代部署/` 目录下的 `index.html` 与 `server.js`
2. 将文件上传至你的服务器 / 任意静态托管平台
3. 安装 Node.js（若使用 server.js）
4. 安装依赖：

```
npm install express node-fetch cors
```

   5.启动服务：

```
node server.js
```

   6.访问：

```
http://localhost:8000
```

------

### 3️⃣ 完全自托管后端（Node.js / Python）

适合已拥有服务器且想**彻底私有化部署**的用户。

------

## 🔧 Node.js 自建后端

下载“完全私有部署\Nodejs环境”目录下的index.html、server.js和allOrigins-main.zip（请自行解压、查看并安装allOrigins）

### 安装依赖

```
npm install express node-fetch cors
```

### 启动服务器

```
node server.js
```

启动成功后输出：

```
反代服务器已启动：http://localhost:8000
```

------

## 🐍 Python 自建后端

下载“完全私有部署\Python环境”目录下的index.html、requirements.txt和server.py

### 安装依赖

确保已安装 Python3，然后在根目录执行：

```
pip install -r requirements.txt
```

> 若无 requirements.txt，请手动安装：
>  `pip install flask requests`（根据你 server.py 中的实际依赖）

### 启动服务器

```
python server.py
```

成功后访问：

```
http://localhost:8000
```

------

## 🌐 示例网站（基于 Cloudflare Worker）

以下示例使用自定义域进行 Worker 反代部署：

- https://ss.iys.pp.ua
- https://ss.ixq.pp.ua

------

## 🖼 页面示例

![网站页面](https://files.catbox.moe/922g74.png)

------

## 🙏 鸣谢

- [Iwara](https://www.iwara.tv)
- [Cloudflare](https://www.cloudflare.com/)
- [SweetAlert](https://sweetalert.js.org/)
- [gnuns](https://github.com/gnuns)

